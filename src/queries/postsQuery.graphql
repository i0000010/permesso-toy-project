query ListPosts {
    posts {
        id
        title
        body
    }
}

mutation CreatePost($title: String!, $body: String!) {
    insert_posts_one(object: {title: $title, body: $body}) {
        id
        title
        body
    }
}